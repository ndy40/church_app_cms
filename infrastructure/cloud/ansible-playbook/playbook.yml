---
- hosts: localhost
  become: yes
  vars_files:
    - vars/docker.yml
    - vars/database.yml
  roles:
    - geerlingguy.docker
    - geerlingguy.postgres

  tasks:
    - name: "install postgres packages"
      apt:
        pkg:
          - postgresql-client-common
        lock_timeout: 180
