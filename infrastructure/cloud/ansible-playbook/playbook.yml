---
- hosts: localhost
  become: yes
  vars_files:
    - vars/docker.yml
  roles:
    - geerlingguy.docker
    - geerlingguy.postgresql

  tasks:
    - name: "install postgres packages"
      apt:
        pkg:
          - postgresql-client-common
        lock_timeout: 180
