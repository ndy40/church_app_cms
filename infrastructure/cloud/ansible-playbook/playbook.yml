---
- hosts: localhost
  become: yes
  vars_files:
    - vars/docker.yml
  roles:
    - role: roles/geerlingguy.docker

  tasks:
    - name: "Install postgres cli client"
      apt:
        - postgresql-client-common
